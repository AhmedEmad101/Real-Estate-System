services:
  server:
    build: .
    container_name: real_estate_app
    volumes:
      - .:/var/www/html
    depends_on:
      - mysql

  nginx:
    image: nginx:latest
    container_name: laravel_realestate_nginx
    ports:
      - "7000:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - server

  mysql:
    image: mysql:8.0
    container_name: laravel_realestate_mysql
    restart: always
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: realestate
    volumes:
      - dbdata:/var/lib/mysql

volumes:
  dbdata: